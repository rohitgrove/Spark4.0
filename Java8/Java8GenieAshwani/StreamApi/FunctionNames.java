public class FunctionNames {
    public static void main(String[] args) {
        /*
            Stream --> This is a Interface, present in java.util.stream
            1. we got stream by invoke stream() on top of collection object
            
            we can process a object in 2 ways
            1.Configuration
                a.Filter -- to filter object on the basis of boolean condition[T/F]
                b.map -- if we want to make a new object for every present in Collection
            2.Processing
                a. count()
                b. min()
                c. max()
                d. toArray()

            Java 8 stream Terminal Operations

            toArray()
            count()
            forEach()
            min()
            max()
            anyMatch()
            allMatch()
            noneMatch()
            findAny()
            findFirst()
            collect()
         */
    }
}
